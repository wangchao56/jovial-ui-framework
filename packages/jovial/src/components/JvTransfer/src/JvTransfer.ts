export const jvTransferProps = {} as const
export interface JvTransferProps {
  // 组件属性定义
};
export interface JvTransferEmits {
  // 事件定义
}
export interface JvTransferSlots {
  // 插槽定义
  default?: () => any
}
export interface JvTransferExpose {
  // 暴露的方法和属性
}
