import { defineComponent } from 'vue'

export default defineComponent({
  name: 'JvInputNumber',
  props: {},
  emits: [],
  setup(props, ctx) {
    return () => {
      return <div>JvInputNumber</div>
    }
  }
})
