export const jvDatePickerProps = {} as const
export interface JvDatePickerProps {
  // 组件属性定义
};
export interface JvDatePickerEmits {
  // 事件定义
}
export interface JvDatePickerSlots {
  // 插槽定义
  default?: () => any
}
export interface JvDatePickerExpose {
  // 暴露的方法和属性
}
