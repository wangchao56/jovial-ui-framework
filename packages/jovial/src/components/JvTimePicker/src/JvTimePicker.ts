export const jvTimePickerProps = {} as const
export interface JvTimePickerProps {
  // 组件属性定义
};
export interface JvTimePickerEmits {
  // 事件定义
}
export interface JvTimePickerSlots {
  // 插槽定义
  default?: () => any
}
export interface JvTimePickerExpose {
  // 暴露的方法和属性
}
