export const jvTimelineProps = {} as const
export interface JvTimelineProps {
  // 组件属性定义
};
export interface JvTimelineEmits {
  // 事件定义
}
export interface JvTimelineSlots {
  // 插槽定义
  default?: () => any
}
export interface JvTimelineExpose {
  // 暴露的方法和属性
}
